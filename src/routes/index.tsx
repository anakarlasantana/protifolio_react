import { Routes, Route } from 'react-router-dom'

function AppRoutes() {
    return (
        <Routes>
            <Route path='/Home' element={<p>Home</p>} />
        </Routes>
    );
}
Route
export default AppRoutes;